import { IndexedByteArray } from "../../../utils/indexed_array.js";

/**
 * @param zone {BasicInstrumentZone}
 * @this {BasicSoundFont}
 * @returns {IndexedByteArray}
 */
export function writeDLSRegion(zone)
{
    return new IndexedByteArray(0);
}